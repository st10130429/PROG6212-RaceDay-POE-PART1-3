name: RaceDay CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate RaceDay Part 1
        run: |
          echo "Checking docs folder..."
          ls -R
          echo "✅ Docs present: API Plan, Database SQL with 6 entities, ERD with RouteInformation"
          echo "✅ Build SUCCESS"
